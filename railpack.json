{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8000}",
    "aptPackages": ["ffmpeg"]
  }
}
